engine-strict=true
# The lock files are resolved without auto-installing peer dependencies (manifold-3d lists glTF and
# esbuild tools it only needs for its own build scripts). Keep every machine and CI the same.
legacy-peer-deps=true
